[Unit]
Description=ContentForge-AI folder watcher (auto Reels pipeline)
After=network.target

[Service]
Type=simple
WorkingDirectory=__REPO__
EnvironmentFile=-__REPO__/.env
ExecStart=__REPO__/.venv/bin/contentforge -q run
Restart=on-failure
RestartSec=10
Nice=10

[Install]
WantedBy=default.target
