# Core runtime
pyyaml>=6.0
pydantic>=2.5
python-dotenv>=1.0
rich>=13.0
watchdog>=3.0
apscheduler>=3.10,<4
pillow>=10.0
numpy>=1.24
opencv-python-headless>=4.8
faster-whisper>=1.0
psutil>=5.9
requests>=2.31

# LLM provider (Gemini recommended)
google-generativeai>=0.8

# Text-to-speech backends (install the ones you use)
piper-tts>=1.2
edge-tts>=6.1
espeakng-loader>=0.2   # bundled libespeak-ng: the always-available offline fallback
# kokoro>=0.9          # optional, heavy (torch). See docs/installation.md
# soundfile>=0.12      # required by kokoro

# OCR for the video-understanding stage (optional).
# Needs the `tesseract` binary too; without it ContentForge uses a built-in
# OpenCV text-region detector (boxes only, no strings) and keeps working.
# pytesseract>=0.3.10

# Dashboard
streamlit>=1.30
pandas>=2.0

# Optional PostgreSQL backend
# sqlalchemy>=2.0
# psycopg2-binary>=2.9
